export interface CropResource {
  crop_type: string;
  farmer_level: number;
  time: number;
  experience: number;
  seed_required: number;
  seed: string;
  location: string;
  min_crop_count: number;
  max_crop_count: number;
}
